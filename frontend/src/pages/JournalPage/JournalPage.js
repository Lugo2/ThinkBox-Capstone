import { useState } from "react";
import axios from "axios";
import useAuth from "../../hooks/useAuth";

//Need to include something that gets the user date

const JournalPage = (props) => {
    
    return(
        null
    );
};
export default JournalPage;